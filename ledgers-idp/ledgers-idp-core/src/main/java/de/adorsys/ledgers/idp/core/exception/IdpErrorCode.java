package de.adorsys.ledgers.idp.core.exception;

public enum IdpErrorCode {
    WRONG_ALGORITHM,
    TOKEN_EXPIRED,
    TOKEN_INVALID,
    TOKEN_SIGNATURE_INVALID,
    FAILED_SIGNING,
    USER_BLOCKED,
    WRONG_CREDENTIALS
}
